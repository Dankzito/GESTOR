<%- include('layout', { title: 'Profesores' }) %>

  <div class="card">
    <h2>Gestor de Profesores</h2>
    <p style="color: #7f8c8d; margin-bottom: 20px;">
      Los profesores se crean cuando un usuario se registra con rol profesor o cuando el administrador cambia el rol de un usuario. El administrador debe validarlos y asignarles materia desde el panel de administración.
    </p>

        <table class="tabla">
          <thead>
            <tr>
              <th>Nombre</th>
              <th>Materia</th>
              <th>Acciones</th>
            </tr>
          </thead>
          <tbody>
            <% profesores.forEach(p=> { %>
              <tr>
                <td>
                  <%= p.nombre %>
                </td>
                <td>
                  <%= p.materia || '-' %>
                </td>
                <td>
                  <% if (user.role==='admin' ) { %>
                    <form style="display:inline" action="/profesores/<%= p.id %>/delete" method="post"><button
                        class="link-btn">Eliminar</button></form>
                    <% } %>
                </td>
              </tr>
              <% }) %>
          </tbody>
        </table>
  </div>
  <%- include('footer') %>